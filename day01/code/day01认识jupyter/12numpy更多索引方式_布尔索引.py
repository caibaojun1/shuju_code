import numpy as np

t2 = np.array(range(24)).reshape(4,6)
print(t2)
#######################布尔索引###############################

# # 把t2里小于10的替换为3
# print(t2<10)
# t2[t2<10] = 3  # 会把位置为True的值进行赋值;
# print(t2)
# print(t2[t2>20])


############################ numpy中的三元运算符 ################################

# obj1 = np.where(t2<3,100,300)  # 判断t2里面的元素是否小于3 t2<3是否成立,成立的话就是100,不成立的的话就是300;
# print(obj1)

t2[t2<10] = 3
print(t2)
t2 = t2.astype(float)
t2[3,3] = np.nan
print(t2.clip(10,18))